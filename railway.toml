# Railway deployment configuration — Umakraft Discord Bot
# https://docs.railway.app/reference/deployments

[build]
  builder = "DOCKERFILE"
  dockerfilePath = "railway/Dockerfile"

[deploy]
  numReplicas = 1
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 5
